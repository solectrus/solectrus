= turbo_stream.update 'forecast-timeframe' do
  = render Timeframe::Component.new(timeframe:, forecast_days:)

= turbo_stream.update 'forecast-chart' do
  = render ChartLoader::Component.new(sensor_name: :inverter_power_forecast, timeframe:)
