- if auto?
  meta name="theme-color" media="(prefers-color-scheme: dark)" content= ThemeConfig::DARK_COLOR data-turbo-permanent= true id= 'theme-color-dark'

/ The tag the theme selector keeps up to date. It carries no media, so it
  applies whenever the tag above does not.
meta name="theme-color" content= color data-turbo-permanent= true id= 'theme-color'
