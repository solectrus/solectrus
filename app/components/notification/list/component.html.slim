.h-full.flex.flex-col
  h1.pb-4.text-gray-500.text-base.sm:text-lg.md:text-xl.lg:text-2xl.flex-shrink-0.hidden.sm:block
    = t('layout.notifications')

  / Negative margin lets the row background reach past the content edge, so the
  / row text still aligns with the heading above.
  .-mx-3.divide-y.divide-gray-200.dark:divide-gray-700.overflow-y-auto.flex-1
    - notifications.each do |notification|
      = render Notification::Item::Component.new(notification:)
